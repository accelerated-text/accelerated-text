image:
    - debian:stretch-slim

services:
    - node:latest
    - alekzonder/puppeteer:1

before_script:
    - apt-get update -qq && apt-get install -y -qq make build-essential

test:preact_demo:
    script:
        - make test
