import { createContext }    from 'preact';


export default createContext({
    closeAll:               null,
    closeDictionaryItem:    null,
    dictionaryItemId:       null,
    openDictionaryItem:     null,
    openHelp:               null,
});
