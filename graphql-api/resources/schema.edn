{:objects
 {:LexiconItem
  {:fields {:createdAt {:type String}
            :updatedAt {:type String}
            :synonyms  {:type (list String)}
            :key       {:type String}}}

  :Lexicon
  {:fields {:offset     {:type Int}
            :totalCount {:type Int}
            :limit      {:type Int}
            :items      {:type (list :LexiconItem)}}}}

 :queries
 {:searchLexicon {:type    :Lexicon
                  :args    {:query  {:type String}
                            :limit  {:type String}
                            :offset {:type String}}
                  :resolve :get-lexicon}}}
